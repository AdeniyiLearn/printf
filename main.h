#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <string.h>

int _putchar(char c);
int _printf(const char *format, ...);
int _strlen(const char *s);
int string_put(char *string);
int digital(int pin);

#endif /* MAIN_H */
